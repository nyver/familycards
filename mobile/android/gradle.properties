org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler computes relative paths between the
# project directory and dependency sources (e.g. in the pub cache). On
# Windows that throws when the two live on different drive letters (this
# project is on E:, the pub cache defaults to C:), so incremental
# compilation is disabled to avoid it.
kotlin.incremental=false
